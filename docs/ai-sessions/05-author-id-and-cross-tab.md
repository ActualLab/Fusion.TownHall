# Session 05

<User> Editing a username doesn't update the author shown under that user's already-posted questions. Fix the root cause: the question API must reference the author by id (not embed the name), and a UI component should resolve the name by id via its own cached call. Audit the whole API for this rule — never aggregate what one extra (free, with Fusion) round trip can fetch. Also add tests: for every command's invalidation block, assert the reads it invalidates actually update (ComputedTest/ReadWhen-style); and separately verify cross-tab UI propagation live using chrome1 + chrome2 (two Chrome instances) — e.g. a title edit in one tab shows up in the other.

<Opus4.8> Replaced the `Question.AuthorName` snapshot with a public `AuthorId`: added a non-secret `ParticipantId` (xxHash of the session id — the raw session id authorizes votes/ownership, so it's never exposed), re-keyed `DbParticipant` to that public id, added `IParticipants.GetName(participantId)` (session-less, so it's cached once and shared), and a `ParticipantName` UI component that resolves the author name per question. Renames now invalidate `GetOwn` + `GetName`, so every question and both tabs update live. API audit: `Question.AuthorName` was the only violation (Trending/RoomStats/lists are already id-based or intrinsic aggregates). Bumped the dev Sqlite file to `TownHall_v2.db` (EnsureCreated doesn't migrate the re-keyed columns). Tests: rewrote the author-snapshot test into author-by-id + rename-propagation, added a direct `GetName`-invalidated-on-rename chain test — 52 green. Verified cross-instance in chrome1→chrome2: renaming the poster updated the question author in the other browser, and a room-title rename updated the heading + live page title in the other browser, both without reload.

