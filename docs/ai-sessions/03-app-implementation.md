# Session 03

<User> Now let's implement the app itself. Read the AI session logs and everything relevant, and implement it. If at some point you decide certain tasks can be done in parallel by weaker models (e.g. Opus or Sonnet), you can definitely spin up agents to do that.

<Fable5> Implemented Phase 1 end to end: contracts (already drafted) got Fusion-compute-service implementations in TownHall.Host (Rooms, Participants, Questions, RoomStats, Mood as `DbServiceBase<AppDbContext>` services with pseudo-dependency + delayed-invalidation patterns; Presence as an in-memory service with a 30 s TTL and scheduled expiry invalidation), Sqlite entities with composite keys, a deterministic "Adjective Animal" name generator, per-tab sessions (`sessionStorage` GUID assigned via `ISessionResolver` after the first interactive render), and the full MudBlazor UI (home page with create form + post-create panel + live room list, owner-claim redirect page, room page with owner bar, composer, Top/Recent/Resolved tabs, SVG mood face + distribution + emoji buttons, trending panel, presence beacon, client-side countdown); used two parallel Explore agents to extract current Fusion/TodoApp API patterns first. Two runtime gotchas fixed during browser verification: Fusion RPC defaults to MemoryPack, so `RpcSerializationFormatResolver.Default = new("msgpack6c")` is set in shared startup (contracts carry only `[MessagePackObject(true)]`), and `StatefulComponentBase` sets `MustRenderAfterEvent = false`, so components with local UI state (editors, toggles, form fields) opt back in via `MustRenderAfterEvent = true`. Verified the whole acceptance walkthrough live in three Chrome tabs (create → start → post → vote → moods → trending → resolve → owner delegation → rename), in both WASM and Server render modes.

<User> By the way, add basic tests for the API — for each method. Use database isolation similar to how it's implemented in the Fusion tests: every test class gets a new uninitialized database. You'll also need a test host that hosts the app on a random port — look at how it's done in the Fusion tests. Write all the helpers needed to implement both DI-level tests (using services from the app's DI container directly) and client-level tests (client-side services + the test host).

<Fable5> A background agent studied Fusion's test infrastructure and added tests/TownHall.Tests (xunit 2.9.3): a per-class `TestAppHost` fixture (unique Sqlite DB in the temp dir, EnsureCreated on start, deleted on dispose; Kestrel on port 0 exposing the Fusion RPC WebSocket endpoint; a lazy client container mirroring ClientStartup) and `TestBase` with a `ReadWhen` invalidation-aware read helper; per-interface abstract test classes instantiated twice — against server DI and against the RPC client — plus a `Computed.Capture`/`WhenInvalidated` invalidation smoke test; all green in ~1 s.

<User> On DateTime: use it on the server and assume it's UTC. For database-stored values, use the same pattern as TodoApp/ActualChat — if there's no Kind, assume UTC; there are helpers for that. More importantly, use Moment everywhere instead of DateTime, unless it's a database entity.

<Fable5> Switched all contract models (Room, Question, Resolution) and service internals to `Moment`; DB entities keep UTC `DateTime` columns, converted on read via `.DefaultKind(DateTimeKind.Utc)` (+ implicit Moment conversion), with `Clocks.SystemClock.Now` as the time source.

<User> To repeat, about sessionStorage sessions: I don't think we need per-tab sessions — we need per-browser sessions. But maybe I misunderstand what it's all about.

<Fable5> Kept per-tab `sessionStorage` sessions and explained why: the Phase 1 spec explicitly requires tabs to act as separate users so a single machine can demo real-time sync between two "participants" side by side (different generated names, separate votes and moods); a per-browser session would collapse all windows into one user — and switching to `localStorage` later is a two-line change if wanted.

<User> Since the home page lists active (and probably recent) town halls, it makes sense to add a Private checkbox for a town hall: owners must be able to check and uncheck it live, and while it's checked the town hall is hidden from the public list.

<Fable5> Added `IsPrivate` end to end: a flag on `Room`/`DbRoom` and `Rooms_Create`, a new owner-only `IRooms.OnSetIsPrivate` command (idempotent, rejected once Ended, invalidates the room + the list), `ListActiveIds` now excludes private rooms, a "Private" checkbox on the create form and in the owner bar (verified live: checking it removes the room from the home page in real time, unchecking brings it back), and Server/Client test pairs for it (41 tests total).

<User> On the question field: multiline wrapping is fine, but it should submit on Enter, and before submitting show a brief dialog that previews the question and asks to confirm (Enter-submit might be unexpected). Questions must not contain explicit line feeds — if the user types or pastes them, turn them into spaces and collapse excessive whitespace into a single paragraph. Besides that, the owner should be able to change the title and increase/decrease the duration in 5-minute increments. The owner link should never be displayed — only copied to the clipboard on click (button named "Copy owner link"), with a toast confirming the copy; the post-create panel shows just the participant link. And make Owner tools a collapsible section.

<Fable5> Composer: Enter now opens a "Post this question?" preview dialog (via `IDialogService.ShowMessageBoxAsync`, triggered on key-up so the same Enter can't also confirm the dialog — the initial key-down version self-confirmed instantly) and both Enter and the Post button go through it; whitespace/line feeds collapse to single spaces on both client and server (`Questions.OnPost`). Added owner-only `Rooms_SetTitle` and `Rooms_AdjustDuration` (ClosesAt clamped to [now, CreatedAt + 24 h]) with UI in the owner bar (Title + Rename, −5/+5 min). Owner links are never rendered: "Copy owner link" buttons in the owner bar and post-create panel copy to the clipboard and show a snackbar toast (participant-link copy toasts too). The owner bar became a collapsed-by-default MudExpansionPanel. All verified live; tests extended to 47.

<User> Also, let's move the render-mode selector (Auto/WASM/Server) to the very top bar of the page, right after the theme toggle.

<Fable5> Compacted `RenderModeSwitch` to a bare menu and moved it into the MudAppBar after the dark-mode toggle; removed it from the home page.

<User> One more important thing: Fusion APIs are supposed to be cache-friendly — an API returning a list (e.g. active town halls) should return just identifiers, and the component displaying each item should fetch it via a separate (cached) API call. Also, to resolve entities by key, use the DB entity resolver where possible — it batches database lookups. Review the API and think what has to be changed.

<Fable5> Reviewed: the contract was already id-based (ListActiveIds/question lists return ids; per-item facets — Get, GetVoteCount, GetResolution, HasOwnVote — are separate compute methods the row components call), so no API shape changes; switched by-key reads to batched `IDbEntityResolver`s — `DbRoom` (GetRoom, GetOwnerToken), `DbParticipant` (GetOwn), and `DbQuestion` (GetQuestion, GetQuestionResolution), which required giving DbQuestion a single-column composed key `"{RoomId}/{Index}"` (the TodoApp `DbTodo` pattern; resolvers can't batch composite keys) plus a unique (RoomId, Index) index; registered the resolvers in Program.cs and the test host; 47 tests green.

<User> In owner tools, the button placement isn't quite logical — explain what +5/−5 minutes means. Also, when a hall ends, the owner should be able to resurrect it by adding time: within a 10-minute grace period, +5 min brings an expired hall back alive, with the end time drifting to now + 5 minutes; display negative time-to-close in that case. The room clock should be bigger for users and turn red (or otherwise stand out) when the hall is closing in seconds or minutes. And the per-question actions layout is weird: show the question text first, then "— by username" on a line below, with the like button at the very end of that same line.

<Fable5> Owner tools reorganized into labeled rows (Start/Stop + Private + Copy owner link; then "Closing time (42 min left): [−5 min][+5 min]" — the label switches to "Ended — +5 min revives it for 5 minutes from now" once ended; then Title + Rename); `OnAdjustDuration` now resurrects an Ended room within a 10-minute grace period (positive delta only, ClosesAt = now + delta); the countdown became a large h5 clock shown while Live or in overtime, displaying negative time ("−0:00:10 overtime") and turning red under 5 minutes; question cards now render text, then "— by author · age" with resolve/delete icons and the like button at the end of the same line. Verified live end to end (shrank a room to death with −5 clicks, watched the red overtime clock, revived it with +5) and added a resurrection test — 49 tests green.

<User> Commit and push everything.

<Fable5> Renamed the session log to 03-app-implementation.md and committed the whole Phase 1 implementation (contracts, Fusion services, DB, UI, tests, README) in a single commit, then pushed to origin/main.
