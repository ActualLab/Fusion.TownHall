# Session 02: Init code

<User> Read the last session log and start `02-init-code.md`. This session's goal: create the initial project template and make sure you can use the browser and a server loop (like in ActualChat) to run and test the app. See https://mudblazor.com/getting-started/installation — use `dotnet new` to create a MudBlazor WASM template in the `src` folder (solution in the root), similarly to how BoardGames is structured. Configure the `.csproj` files similarly to Fusion Samples, i.e. centralized package versioning and `Directory.Build.props` files in `src` and `tests` folders (don't copy every option yet, just what makes sense). Finally, replicate the `/server-loop` skill from ActualChat here — copy `server-loop.ps1` and `.cmd`, adjust them for this project, run the loop, and make sure the app is accessible via the chrome1 MCP.

<Fable5> Generated the MudBlazor Web App template (WASM interactivity, all-interactive, no HTTPS) into `src/` as `TownHall` (host) + `TownHall.Client` (WASM) with `TownHall.slnx` in the root; added Fusion-Samples-style `global.json`, root `Directory.Build.props` (net10.0, CPM, artifacts output), `Directory.Packages.props`, and `src`/`tests` `Directory.Build.props`; ported `server-loop.ps1`/`.cmd` (2 steps — dotnet-build + server-run of the built exe on `http://localhost:5136`, watchdog probe, restart via `tmp/server-loop.restart` marker) plus an adapted `.claude/commands/server-loop.md`; fixed the template's dangling `TownHall.styles.css` link (404); verified via chrome1 MCP — home page renders, Counter increments (WASM works), restart marker rebuilds and relaunches, console clean.

<User> Get rid of every template page except Home; give the app its proper name, TownHall; and update the home page to display roughly what's in the README. Also add a rule: one AI session file per commit — when starting a session, if the latest session file has no uncommitted changes, create a new file like `03.md` (it gets its descriptive suffix, e.g. `03-basic-app.md`, right before the commit); if the latest session file has uncommitted changes, the commit hasn't happened yet, so append to it. When in doubt about committing (or about appending vs. creating a new session file), ask what to do with the session log.

<Fable5> Deleted `Counter.razor`/`Weather.razor` and their nav links, renamed the app bar title to "TownHall" (also dropped the dead menu button), rewrote `Home.razor` with the README content (description, comparison purpose, Phase 1 note, related-repo links), added the one-session-file-per-commit rule to `AGENTS-Source.md`, and regenerated `AGENTS.md`/`CLAUDE.md` via `ai update-md`.

<User> Also rename the server project to `TownHall.Host` and the WASM UI project to `TownHall.UI` — folders and namespaces too.

<Fable5> Renamed folders/csproj files to `src/TownHall.Host` and `src/TownHall.UI`, updated the solution, project reference, `Program.cs` usings, `typeof(TownHall.UI._Imports)`, both `_Imports.razor` files, and `server-loop.ps1`/`.claude/commands/server-loop.md` (binary is now `artifacts/bin/TownHall.Host/...`); restarted the loop process and re-verified in Chrome — new TownHall home page renders, console clean.

<User> Finally, add a DI setup similar to TodoApp's: add Fusion; assume we'll use a Sqlite DB for now — use the DB setup from TodoApp, but without multitenancy. Adopt TodoApp's Blazor setup in terms of publish/trimming and pages (`_XxxPage.razor` files, etc.) — "copy" means adopt. Assume there will be no authentication. To verify Fusion works, display a component showing the current time somewhere on the home page.

<Fable5> Adopted the TodoApp setup: added `TownHall.Contracts` (with `ITime` compute-service interface), Fusion + EF packages via central versioning, Fusion global usings in `Directory.Build.props`; Host got the TodoApp-style `Program.cs` (Fusion web server + RPC WebSocket server, `AddDbContextServices` with operations framework + file-system log watcher on Sqlite in the app temp dir, no tenants/auth, EnsureCreated on start), `HostSettings`, `AppDbContext`, `TimeService`, and `_HostPage`/`_ErrorPage` components with render-mode + session wiring; UI got TodoApp's trimming/publish csproj settings (full trim, trimmer roots, invariant globalization), `ClientStartup` (WASM RPC client + shared MudBlazor/Fusion Blazor services), a `CircuitHubComponentBase`-based `App.razor`, and a `ServerTimeBadge` (`ComputedStateComponent<DateTime>`) on the home page; verified in Chrome — server time ticks live (1 s auto-invalidation), console clean, Sqlite DB created.

<User> Since I decided to add Fusion, the plan changes: our first implementation will be the Fusion-based one; the other implementations will be figured out later, starting from the Fusion version. Make two changes: update `docs/TownHall-Phase1.md` to describe that the Fusion-based version is implemented first, and update the home page to reflect it; change the README too if needed.

<Fable5> Reworked `docs/TownHall-Phase1.md` for the Fusion-first plan: intro now states Phase 1 is the Fusion implementation; sessions ride on Fusion `Session` (per-tab GUID via `ISessionResolver`); the API contract preamble describes compute services + Fusion RPC instead of HTTP endpoints/X-Session; refresh controls and refresh semantics were replaced with real-time update semantics; storage switched from in-memory to the already-wired Sqlite/EF + operations framework; non-goals, project layout (`TownHall.Contracts`/`Host`/`UI`), and the acceptance walkthrough updated accordingly; README and the home page alert now say the Fusion version is built first and updates in real time.

<User> Also borrow TodoApp's mechanics for switching between Blazor Server and WebAssembly; put the switch on the home page for now. If TodoApp doesn't expose Auto mode, expose it here — we need it.

<Fable5> The render-mode endpoints and `_HostPage` wiring were already adopted, so added a MudBlazor `RenderModeSwitch` (menu over `RenderModeDef.All` + `RenderModeHelper`, same as TodoApp's `BarRenderMode` — Auto is exposed and is the default) to the home page, and verified all modes in Chrome: Auto (Server) → WASM (clock still ticks over the WASM RPC client) → back to Auto, which now upgrades to Auto (WASM).

<User> In the server-loop skill, mention that the loop is supposed to have a PID file while running — and if inspecting it shows the loop is not running, it's fine for you to start it yourself and keep it running while you work.

<Fable5> Added a `tmp/server-loop.pid` file to `server-loop.ps1` (written on startup) and a "Is the loop running at all?" section to `.claude/commands/server-loop.md` — check the PID file / process liveness, and if the loop isn't running, start it yourself as a background task; restarted the loop and verified the PID file points at the live pwsh process.

<User> One more thing: if you're running in Docker, the PID check won't work (different PID namespace) and you can't start a host process — in that case the user has to start server-loop manually, and you should ask them to. Only start it yourself when running on the host OS.

<Fable5> Split the skill's "loop not running" guidance by environment (via `AC_OS`): on the host OS the agent starts the loop itself in the background; in Docker/WSL the PID check is unreliable (use an HTTP probe as a hint) and the agent must ask the user to run `server-loop.cmd` on the host.
